version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run:
          command: |
            echo "RELEASE_TAG = $RELEASE_TAG"
            echo "RELEASE_TAG_PROD = $RELEASE_TAG_PROD"
            echo "CIRCLE_TAG = $CIRCLE_TAG"
            [[ -n $CIRCLE_TAG ]] || export CIRCLE_TAG=$(git describe --always --tags)
            echo "CIRCLE_TAG = $CIRCLE_TAG"
workflows:
  version: 2
  build:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/
